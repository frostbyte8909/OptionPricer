[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "optionpricer"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "scipy",
]

[project.optional-dependencies]
fast = ["py_lets_be_rational"]
